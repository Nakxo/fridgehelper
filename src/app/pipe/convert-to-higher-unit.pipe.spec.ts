import { ConvertToHigherUnitPipe } from './convert-to-higher-unit.pipe';

describe('ConvertToHigherUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToHigherUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
